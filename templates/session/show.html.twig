{% extends 'base.html.twig' %}

{% block title %}Session{% endblock %}

{% block body %}
    <h1>Session</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ session.id }}</td>
            </tr>
            <tr>
                <th>Distance</th>
                <td>{{ session.distance }}</td>
            </tr>
            <tr>
                <th>Heuredeb</th>
                <td>{{ session.heuredeb ? session.heuredeb|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Heurefin</th>
                <td>{{ session.heurefin ? session.heurefin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Vitessemax</th>
                <td>{{ session.vitessemax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_session_index') }}">back to list</a>

    <a href="{{ path('app_session_edit', {'id': session.id}) }}">edit</a>

    {{ include('session/_delete_form.html.twig') }}
{% endblock %}
